## Custome commands
# turn off postgresql

# Make it so it doesn't start at boot. 

# Create pg.conf

# Intitilize under /db/postgresql/replication and cluster

# Put in system.d start file

# set to off by default for both at start
